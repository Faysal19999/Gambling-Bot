"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveDate = void 0;
const result_1 = require("@sapphire/result");
require("../errors/Identifiers");
function resolveDate(parameter, options) {
    const parsed = new Date(parameter);
    const time = parsed.getTime();
    if (Number.isNaN(time)) {
        return result_1.Result.err("dateError" /* Identifiers.ArgumentDateError */);
    }
    if (typeof options?.minimum === 'number' && time < options.minimum) {
        return result_1.Result.err("dateTooEarly" /* Identifiers.ArgumentDateTooEarly */);
    }
    if (typeof options?.maximum === 'number' && time > options.maximum) {
        return result_1.Result.err("dateTooFar" /* Identifiers.ArgumentDateTooFar */);
    }
    return result_1.Result.ok(parsed);
}
exports.resolveDate = resolveDate;
//# sourceMappingURL=date.js.map